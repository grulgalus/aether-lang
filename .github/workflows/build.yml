name: Aether CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run Build
      run: echo "Zde se bude kompilovat Aether přes LLVM!"
