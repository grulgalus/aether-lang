actor Worker {
    fn initialize() {
        let text = "Aether " + "kalkulačka:"
        print(text)

        let x = 10 * 5
        print("10 * 5 = ")
        print(x)
        
        let y = 100 - 45
        print("100 - 45 = ")
        print(y)
        
        return x
    }
}
